[build]
  command = "cd client && npm install && npm run build"
  publish = "client/build"

[[redirects]]
  from = "/api/*"
  to = "http://localhost:3001/:splat"
  status = 200

[build.environment]
  NODE_ENV = "production"
